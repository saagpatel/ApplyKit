name: perf-foundation
# parity guard: bash ./.codex/scripts/run_perf_foundation.sh

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main, master]
  workflow_dispatch:

jobs:
  perf-foundation:
    uses: saagpatel/sovereign/.github/workflows/perf-foundation.yml@345c412bfc011f4ec602eadc42fd420dec62e85e
