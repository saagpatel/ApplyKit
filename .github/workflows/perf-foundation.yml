name: perf-foundation
# parity guard: bash ./.codex/scripts/run_perf_foundation.sh

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main, master]
  workflow_dispatch:

jobs:
  perf-foundation:
    uses: saagpatel/sovereign/.github/workflows/perf-foundation.yml@f27de0d13192c0ae42e94cdec85b8c224971bc8a
