name: git-hygiene

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main, master]
  workflow_dispatch:

jobs:
  git-hygiene:
    uses: saagpatel/sovereign/.github/workflows/git-hygiene.yml@345c412bfc011f4ec602eadc42fd420dec62e85e
