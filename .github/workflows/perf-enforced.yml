name: perf-enforced
# parity guard: bash ./.codex/scripts/run_perf_enforced.sh

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main, master]
  workflow_dispatch:

jobs:
  perf-enforced:
    uses: saagpatel/sovereign/.github/workflows/perf-enforced.yml@f27de0d13192c0ae42e94cdec85b8c224971bc8a
