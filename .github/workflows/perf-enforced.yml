name: perf-enforced
# parity guard: bash ./.codex/scripts/run_perf_enforced.sh

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main, master]
  workflow_dispatch:

jobs:
  perf-enforced:
    uses: saagpatel/sovereign/.github/workflows/perf-enforced.yml@345c412bfc011f4ec602eadc42fd420dec62e85e
