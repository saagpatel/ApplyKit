# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass,
which treats the code as read-only ground truth and edits only documentation so it
reflects the repo's actual current state. It changed no source code, config, tests,
or lockfiles, and ran no builds or tests. Evidence was gathered by reading source
files, manifests, CI workflows, and git history.

---

## Per-Claim Findings

### 1. What it is
**Status:** `consistent`
**Evidence:** `verified-by-reading-code`
- `crates/applykit_core/src/truth_gate.rs` exists; `pipeline.rs` exports `generate_packet`.
- `crates/applykit_core/src/determinism.rs` exists, consistent with "deterministic output" claim.
- README intro accurately describes the project.

### 2. Current state
**Status:** `drifted` (partial — features list incomplete)
**Evidence:** `verified-by-reading-code`
- `crates/applykit_export/src/lib.rs` implements `export_docx` and `export_pdf` (lines 256–316, 300–315).
- `docs/phases.md` lists "Phase 6: DOCX/PDF export" as a planned phase; the code confirms it is implemented.
- README Features listed "Full application packet" but omitted DOCX and PDF export, which are real and tested.
- **Change:** Added bullet `**Export formats** — DOCX and PDF export of each generated packet` to README Features.

### 3. Stack
**Status:** `consistent`
**Evidence:** `verified-by-reading-code`
- `Cargo.toml` workspace members: `applykit_core`, `applykit_llm`, `applykit_export`, `applykit_cli`, `src-tauri` — matches README table.
- `src-tauri/Cargo.toml` depends on `tauri = "2.9"` — matches "Tauri 2" claim.
- `crates/applykit_llm/src/lib.rs`: `OllamaAdapter` and `OpenAiCompatAdapter` — matches "Ollama / LM Studio (local) via reqwest".
- `crates/applykit_core/src/storage.rs` uses `rusqlite` — matches "SQLite via rusqlite".
- `ui/package.json` has `"react": "^19.2.5"` and `"@tauri-apps/api": "^2.11.0"` — matches "React + TypeScript" claim.

### 4. How to run
**Status:** `drifted` (two issues)
**Evidence:** `verified-by-reading-code`

**Issue A — wrong desktop run command:**
- README and AGENTS.md both said `pnpm tauri dev`.
- `ui/package.json` has no `@tauri-apps/cli` in devDependencies; root `package.json` has no devDependencies.
- `.github/workflows/ci.yml` line 49–50: `cargo install tauri-cli --version "^2.0" --locked` — tauri CLI is installed via cargo, not pnpm.
- `docs/release-runbook.md` line 149: `desktop_launch_result: pass (cargo tauri dev)` — confirms `cargo tauri dev` is the real command.
- **Change (README):** `pnpm tauri dev` → `cargo tauri dev` in the Usage section.
- **Change (AGENTS.md):** Same fix in the "How To Run" block inside `<!-- portfolio-context -->`.

**Issue B — missing tauri-cli prerequisite:**
- README Prerequisites section listed Rust toolchain, Node.js, and Ollama but omitted tauri-cli.
- CI installs it explicitly: `cargo install tauri-cli --version "^2.0" --locked`.
- Without it, `cargo tauri dev` fails.
- **Change (README):** Added `Tauri CLI (cargo install tauri-cli --version "^2.0")` to the Prerequisites list.

### 5. Known risks
**Status:** `consistent`
**Evidence:** `verified-by-reading-code`
- README has no "Known Risks" section; AGENTS.md notes "minimum-viable recovery context" — neither makes a verifiably wrong claim.

### 6. Next move
**Status:** `unverifiable`
**Evidence:** `unverifiable-because-forward-looking`
- `docs/phases.md` describes phases 0–7; `docs/roadmap-2026q1.md` shows phases 2–5 as complete per their own evidence snapshots. Phase 6 (DOCX/PDF) is implemented in code. Phase 7 (hardening) has partial evidence. Forward-looking phasing is not determinable from the code alone.
- README makes no explicit "next phase" claim, so nothing to correct.

---

## Contradictions for Manual Review

The following files outside the editable set (`README.md`, `CLAUDE.md`, `AGENTS.md`,
`DOC-RECONCILIATION.md`, `docs/`) contain no actionable contradictions — they are
historical evidence logs or planning artifacts accurately labeled as such. No manual
fixes required.

One observation: `SESSION-PLAN.md` (root, line 10–13) says "no roadmap file exists"
and describes an early scaffolding state. This was generated by an auto-tool on
2026-03-14 and is stale relative to the actual developed codebase. It is a non-doc
file and was not edited. A human may want to delete or update it.

---

## Footer

**Generated:** 2026-05-30 20:12:48 PDT
**Branch:** `docs/truth-up-2026-05-30`
**HEAD reconciled against:** `92329670266d067ece75e628d6c97a4c5ab450b1`
